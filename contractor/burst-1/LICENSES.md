# Лицензии сторонних элементов

По одной строке на каждый сторонний ассет (модель, текстура, волосы, одежда, HDRI, шрифт):
что это, откуда (ссылка), лицензия, разрешает ли коммерческое использование и модификацию,
где лежит в файле. Сделанное с нуля тоже перечисли с пометкой «своё».

| Элемент | Источник | Лицензия | Коммерческое / модификация | Где в файле |
|---|---|---|---|---|
| Базовый меш тела и головы MakeHuman (hm08), макро- и модельные таргеты, expression unit targets (набор `caucasian`), helper-геометрия зубов и языка, размер/положение глаз по helper'ам, скелет `default` с весами | MPFB2 `v2.0.17` = коммит `80919fa`, https://github.com/makehumancommunity/mpfb2, `LICENSE.ASSETS.md` | CC0 1.0 (ассеты); код add-on'а GPL-3.0 — в сдачу не входит, используется только при сборке | да / да | `HERO_BODY` (с удалённой под одеждой частью), `HERO_HAND_FG`, `HERO_HEAD` (шейпкеи `fc_*`), `HERO_TEETH_*`, `HERO_TONGUE`, `RIG_HERO`, а также веса `HERO_DRESS` / `HERO_SLEEVE_*` (перенос от ближайшей вершины тела) |
| Своё, скриптом `deliverables/scripts/build_character.py`: кольцо `SOCKET_BOUNDARY` и градиент весов шеи; `RIG_HERO` (переименование, осевая цепочка, досборка); маппинг 26 каналов `FACE_CTRL`, in-between века и драйверы `combination_rules`; глаза `HERO_EYE_L/R` (сфера с зрачком и радужкой геометрией); брови `HERO_BROWS` (параметрическая дуга); волосы `HERO_HAIR` (шлем + 61 прядь); одежда отдельной геометрией `HERO_DRESS` (труба по сечениям, кокетка и горловина с заходом в шею) и `HERO_SLEEVE_L/R`; обувь как регион материала `HERO_SHOES`; прокси; все материалы — процедурные Principled, без текстур | своё | — | — | по контракту |
